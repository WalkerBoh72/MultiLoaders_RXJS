export interface location {description: string, name: string, region: string, id: string, image:string}

export interface heroclass {description: string, name: string,id: string, image:string}

export interface NPC {description: string, name: string, region: string, id: string, image:string, location:string, quote: string}

